How Virtual Threads Work
- it will work like a thread
- it will accept runnable task
- we can do thread.start()/ thread.join()
- but OS cannot see these virtual threads

If OS cannot see these virtual threads, then how they work?
